# ==============================================================================
# ENVIRONMENT VARIABLES - YouTube Automation System
# ==============================================================================
# Copy this file to .env and fill in your actual credentials
# .env file is gitignored for security
# ==============================================================================

# ----------------------------------------------------------------------------
# OPENAI API KEY (Required)
# ----------------------------------------------------------------------------
# Get from: https://platform.openai.com/api-keys
# Used for: GPT-4 scripts, DALL-E images, Whisper transcription
OPENAI_API_KEY=sk-your-openai-api-key-here

# ----------------------------------------------------------------------------
# PIXABAY API KEY (Optional but Recommended)
# ----------------------------------------------------------------------------
# Get from: https://pixabay.com/api/docs/
# Free tier: 5,000 requests/month
# Used for: Stock video footage for B-roll
PIXABAY_API_KEY=your-pixabay-key-here

# ----------------------------------------------------------------------------
# YOUTUBE API CREDENTIALS (Required)
# ----------------------------------------------------------------------------
# Setup Instructions:
# 1. Go to: https://console.cloud.google.com
# 2. Create a new project or select existing
# 3. Enable "YouTube Data API v3"
# 4. Create OAuth 2.0 credentials (Desktop App)
# 5. Download client_secret.json to project root
# 6. Run: python -c "from services.youtube_uploader import generate_token_headless; generate_token_headless()"
# 7. Follow browser authorization flow
# 8. token.pickle will be created automatically

YOUTUBE_CLIENT_SECRET_FILE=client_secret.json
YOUTUBE_TOKEN_FILE=token.pickle

# ----------------------------------------------------------------------------
# SYSTEM CONFIGURATION (Optional)
# ----------------------------------------------------------------------------
# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Maximum concurrent downloads
MAX_CONCURRENT_DOWNLOADS=3

# Video quality (low, medium, high)
VIDEO_QUALITY=high

# FFmpeg threads (auto, or number like 4)
FFMPEG_THREADS=auto

# ----------------------------------------------------------------------------
# ADVANCED OPTIONS (Optional)
# ----------------------------------------------------------------------------
# Enable debug mode (set to '1' to enable)
DEBUG_MODE=0

# Custom output directory (absolute path)
# OUTPUT_DIR=/custom/path/to/videos

# Custom temp directory (for large VPS with separate SSD)
# TEMP_DIR=/tmp/yt-automation

# ==============================================================================
# SECURITY NOTES
# ==============================================================================
# - NEVER commit .env files to version control
# - Use environment variables on VPS (export VAR=value in ~/.bashrc)
# - Rotate API keys periodically
# - Use separate API keys for dev/prod environments
# ==============================================================================
